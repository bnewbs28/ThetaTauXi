jQuery(function( $ ) {
    "use strict"

    $( '#hero-slider' ).slick({
                arrows: false,
                pauseOnHover: true,
                fade: true,
                autoplay: true,
                autoplaySpeed: 7000,
                speed: 1000
            });

});
